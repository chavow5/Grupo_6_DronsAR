<%- include('../partials/head') %>

<style>
  .input-group {
    margin-bottom: 15px;
  }

  .input-group label {
    display: block;
    margin-bottom: 5px;
    font-weight: bold;
  }

  .input-group input {
    width: 100%;
    padding: 10px;
    border: 1px solid #ccc;
    border-radius: 4px;
  }

  .error-message {
    color: red;
    font-size: 12px;
    margin-top: 5px;
  }
</style>

<body>
  <div class="container">
    <div class="login-box">
      <div class="logo-login">
        <img src="/img/DRONSAR_PROP-10.svg" class="login-image" alt="Logo">
      </div>
      <h1>Bienvenido de nuevo</h1>
      <p>¿No tienes una cuenta? <a href="registro">Crea una cuenta</a></p>
      <form name="sentMessage" id="contactForm" action="users/login" method="POST" novalidate="novalidate">
        <div class="control-group mb-3">
            <input type="email" class="form-control" name="email" id="email" placeholder="Tu Email" required="required" data-validation-required-message="Por favor, ingresa tu email" value="<%= email || '' %>" />
            <% if (typeof errors !== 'undefined' && errors.email) { %>
                <p class="help-block text-danger"><%= errors.email.msg %></p>
            <% } %>
        </div>
        <div class="control-group mb-3">
            <input type="password" class="form-control" name="password" id="password" placeholder="Contraseña" required="required" data-validation-required-message="Por favor, ingresa tu contraseña" />
            <% if (typeof errors !== 'undefined' && errors.password) { %>
                <p class="help-block text-danger"><%= errors.password.msg %></p>
            <% } %>
        </div>
        <div>
            <button class="btn btn-primary py-2 px-4" type="submit" id="sendMessageButton">Iniciar Sesión</button>
        </div>
    </form>

      <div class="social-login">
        <p>O continuar con:</p>
        <a href="#" class="social-link"><i class="fab fa-google"></i> Google </a>
        <a href="#" class="social-link"><i class="fab fa-facebook"></i> Facebook </a>
        <a href="#" class="social-link"><i class="fab fa-apple"></i> Apple </a>
      </div>
    </div>
  </div>


  <script>
    /*
    function validateForm() {
      const email = document.getElementById('email').value.trim();
      const password = document.getElementById('password').value.trim();

      let isValid = true;

      // Validación de correo electrónico
      const emailPattern = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
      if (!email) {
        document.getElementById('email-error').innerText = 'El campo de correo electrónico es obligatorio.';
        isValid = false;
      } else if (!emailPattern.test(email)) {
        document.getElementById('email-error').innerText = 'Por favor, ingresa un correo electrónico válido.';
        isValid = false;
      } else {
        document.getElementById('email-error').innerText = ''; 
      }

      // Validación de contraseña
      if (!password) {
        document.getElementById('password-error').innerText = 'El campo de contraseña es obligatorio.';
        isValid = false;
      } else {
        document.getElementById('password-error').innerText = ''; 
      }

      return isValid;
    }
      */
  </script>

</body>
</html>